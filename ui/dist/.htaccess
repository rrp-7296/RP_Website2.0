<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Social Media Open Graph Share Cards
  RewriteRule ^share/([a-z]+)/([0-9]+)$ api/share.php?type=$1&id=$2 [QSA,L]

  # Automatically route /api requests to api/index.php if not a real file/folder
  RewriteCond %{REQUEST_URI} ^/api/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^api/(.*)$ api/index.php [QSA,L]

  # If requested file or directory exists, serve directly
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Otherwise fallback to index.html for React SPA routing
  RewriteRule ^ index.html [L]
</IfModule>
